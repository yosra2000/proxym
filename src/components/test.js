import React from 'react';
import {Button, h1,  p , div } from "react-bootstrap";

const test = () => 
{
    return {
        <div>
        <h1> home page</h1>
        <p> welcome</p>
        <button onClick={this.getData}> submit </button>
        </div>

    }
}